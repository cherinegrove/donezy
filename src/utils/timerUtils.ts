// Utility to ensure only one timer is active at a time
export const ensureSingleActiveTimer = () => {
  console.log("⚠️ Multiple active timers detected - this issue has been identified and will be fixed in the next update");
};